<section id="details-page">

    <div class="main_card">
        <div class="card_left">
            <div class="card_datails">

                <h1>Title: {{item.title}}</h1>
                <h3>Created by an author: {{autorNames}}</h3>
                <div class="card_animal">
                    <p class="card-keyword">Keyword: {{item.keyword}}</p>
                    <p class="card-location">Location: {{item.location}}</p>
                    <p class="card-date">Date: {{item.dateOfCreation}}</p>
                </div>

                <p class="disc">{{item.description}}</p>

                {{#if isAuthenticated}}
                <div class="social-btn">
                    {{#if isOwner}}
                    <a href="/item/{{item._id}}/edit" class="edit-btn">Edit</a>
                    <a href="/item/{{item._id}}/delete" class="del-btn">Delete</a>
                    {{else}}
                    {{#if isLiked}}
                    <p class="thanks-for-vote">Thanks For Voting</p>
                    {{else}}
                    <a href="/item/{{item._id}}/upLike" class="vote-up">UpVote +1</a>
                    <a href="/item/{{item._id}}/downLike" class="vote-down">DownVote -1</a>
                    {{/if}}
                    {{/if}}
                </div>
                {{/if}}
            </div>
        </div>
        <div class="card_right">
            <img src="{{item.imageUrl}}" alt="{{item.title}}">
        </div>
    </div>

</section>

<section id="votes">
    <div class="vote-info">
        <div class="card_left">
            <div class="card_datails">
                <h1>Votes</h1>
                <div class="card_vote">
                    <p class="PV">Total rating of votes: {{item.rating}}</p>
                </div>
                {{#if users}}
                <p class="disc">People who voted for the post - {{usersList}}</p>
                {{else}}
                <p class="disc">People who voted for the post - No one has voted yet.</p>
                {{/if}}
            </div>
        </div>
    </div>
</section>